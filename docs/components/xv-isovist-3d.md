# XV Isovist 3D

Traces a complete equal-solid-angle Fibonacci sphere from every eye. It returns radial volume, radial surface measure, transmission-weighted openness, convergence, endpoints/transmission, ranked Object-ID obstruction, exact category partitions, and exact remove-one volume recovery.

`Radial Surface` is `Σ(ωr²)`, not triangulated envelope area. Increase Samples until `ΔV` is acceptable for the decision. Counterfactual count multiplies work because each selected object is excluded and the original ray set is traced again. Connect the returned Object IDs to `XV Highlight`.

See [the scientific contract](../validation/daena-spatial-solar-intelligence.md).
