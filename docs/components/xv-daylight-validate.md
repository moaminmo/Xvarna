# XV Daylight Δ

Compares aligned Fast Path and Radiance lux arrays. Outputs signed bias, MAE, RMSE, MAPE, maximum absolute error, R², accepted fraction, an all-pairs acceptance flag, and a deterministic hash. Acceptance uses `max(absolute tolerance, relative tolerance × reference)` per pair.
