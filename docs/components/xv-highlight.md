# XV Highlight

Maps attribution Object IDs back to the source Rhino meshes used to create the scene. Selected meshes are previewed with the highlight material, non-selected meshes with the normal material, and both groups remain bakeable outputs.

Pass the exact source Object-ID list alongside the meshes. Missing requested IDs are reported explicitly; geometric matching or name guessing is never used.
