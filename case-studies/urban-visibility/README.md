# Urban visibility and privacy case

Run packaged tutorial `03-daena-view-analysis.ghx` on a consented urban/site model. Record coordinate origin and category mapping; export Target/Green View, privacy conflicts, top-K blockers, counterfactual deltas and HTML report. Public use requires the model source/license/hash in `manifest.json`.

